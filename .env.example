# 이 파일을 복사해 .env.local 로 만들고 값을 채우세요. (.env.local 은 git에 커밋되지 않습니다)
# 모두 선택 사항입니다 — 비워두면 로컬 데이터(src/data)만으로 사이트가 정상 동작합니다.

# (선택) Supabase: 관리자 편집 내용을 서버에 저장하려는 경우에만 필요
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# (선택) Gemini AI: 일부 AI 기능을 사용할 경우에만 필요
GEMINI_API_KEY=""
